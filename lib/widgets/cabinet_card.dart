import 'package:flutter/material.dart';

class CabinetCard extends StatelessWidget {
  const CabinetCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('card');
  }
}
